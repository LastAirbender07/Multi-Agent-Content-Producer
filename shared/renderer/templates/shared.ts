// This file is kept for backward compatibility.
// All component factories have moved to the shared/ subfolder.
// Existing imports like `import { createBrandBar } from "./shared"` continue to work unchanged.
export * from "./shared/index";
